{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "statusbar-window",
  "description": "Minimal surface for the status bar: listen to cross-window app events, read observation status, and drag ITSELF (so the user can move the bar) plus save its own position. Deliberately excludes store writes, connectors, filesystem, shell, and HTTP.",
  "windows": ["statusbar"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:window:allow-start-dragging"
  ]
}
