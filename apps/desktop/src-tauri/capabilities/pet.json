{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pet-window",
  "description": "Minimal surface for the pet window: drag itself, cross-window events, and the settings/panel commands. Deliberately excludes filesystem, shell, HTTP, and any future connector-token capability.",
  "windows": ["pet"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:event:allow-emit",
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
