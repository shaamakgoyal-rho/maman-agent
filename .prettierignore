node_modules
dist
build
.next
.turbo
coverage
target
pnpm-lock.yaml
docs/api/openapi.json
packages/contracts/schemas
# Generated from domain/packs/*.yaml by `pnpm packs:generate`; packs:check enforces
# byte-equality with the generator, so prettier must not reformat them.
domain/packs/*.json
domain/classifier-conformance.json
# Same reason: generated by `pnpm teach:egress-conformance` and byte-compared by
# both the TS test and the Swift runner. Prettier collapses its nested arrays
# differently from JSON.stringify, which broke the drift contract on first run.
domain/teach-egress-conformance.json
packages/domain-packs/src/packs.generated.ts
apps/desktop/src-tauri/gen
# Vendored Seedy source — keep byte-identical to the upstream provenance package.
apps/desktop/src/pet/assets/seedy-source
playwright-report
test-results
.build
